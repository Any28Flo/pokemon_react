import React from 'react';

const Spinner = () => {
    return (
        <div className="wrapper">
            <div className="pokeball">
            </div>
        </div>
    );
};

export default Spinner;